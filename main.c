#include <stdio.h> //

int main() { //without the main fucntion we can't execute a c programm

 //lesson 2 variables
 //A variabke is a reusable container for a value


//floating numbers
//we use these for numbers that have floating/decimal numbers
float price = 10.5;

//when

printf("sugar is %f. per unit\n", price);
    return 0; //you will always see the return  in a main function because it is expected to return an interger it returns it to operating system and zero means a sucess and non zero means an error
}